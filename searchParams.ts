const {
    DEFAULT_QUERY,
    buildSerializableSearchParams,
    createDefaultSearchQuery,
    deriveDefaultCarryRoles,
    deriveDefaultTankRoles,
    normalizeBoolean,
    normalizeSearchParams,
    normalizeSearchParamsForData,
    normalizeStringList,
    normalizeStringMap,
    serializeSearchParams,
    summarizeSearchParams,
    withDefaultRoleFilters
} = require('./src/shared/board-search-query.js');

module.exports = {
    DEFAULT_QUERY,
    buildSerializableSearchParams,
    createDefaultSearchQuery,
    deriveDefaultCarryRoles,
    deriveDefaultTankRoles,
    normalizeBoolean,
    normalizeSearchParams,
    normalizeSearchParamsForData,
    normalizeStringList,
    normalizeStringMap,
    serializeSearchParams,
    summarizeSearchParams,
    withDefaultRoleFilters
};
